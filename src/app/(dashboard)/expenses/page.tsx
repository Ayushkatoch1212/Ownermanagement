"use client";
import{useEffect,useState}from"react";import{Box,Button,Card,CardContent,Dialog,DialogTitle,DialogContent,DialogActions,TextField,Typography,Table,TableBody,TableCell,TableHead,TableRow,MenuItem}from"@mui/material";import AddIcon from"@mui/icons-material/Add";import{money}from"@/lib/utils";
export default function Expenses(){const[data,setData]=useState<any[]>([]),[open,setOpen]=useState(false),[v,setV]=useState<any>({title:"",category:"Maintenance",amount:0,date:new Date().toISOString().slice(0,10),description:""});async function load(){setData((await(await fetch("/api/expenses")).json()).data||[])}useEffect(()=>{load()},[]);async function save(){const r=await fetch("/api/expenses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});if(r.ok){setOpen(false);load()}}return <Box><Box sx={{display:"flex",justifyContent:"space-between",mb:3}}><Box><Typography variant="h4" fontWeight={800}>Expenses</Typography><Typography color="text.secondary">Track operating costs.</Typography></Box><Button variant="contained" startIcon={<AddIcon/>} onClick={()=>setOpen(true)}>Add Expense</Button></Box><Card variant="outlined"><CardContent><Table><TableHead><TableRow><TableCell>Title</TableCell><TableCell>Category</TableCell><TableCell>Date</TableCell><TableCell>Amount</TableCell></TableRow></TableHead><TableBody>{data.map(x=><TableRow key={x._id}><TableCell>{x.title}</TableCell><TableCell>{x.category}</TableCell><TableCell>{new Date(x.date).toLocaleDateString()}</TableCell><TableCell>{money(x.amount)}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card><Dialog open={open} onClose={()=>setOpen(false)} fullWidth maxWidth="sm"><DialogTitle>Add Expense</DialogTitle><DialogContent sx={{display:"grid",gap:2,pt:2}}><TextField label="Title" value={v.title} onChange={e=>setV({...v,title:e.target.value})}/><TextField select label="Category" value={v.category} onChange={e=>setV({...v,category:e.target.value})}>{["Electricity","Water","Internet","Maintenance","Cleaning","Staff Salary","Repairs","Furniture","Other"].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField type="number" label="Amount" value={v.amount} onChange={e=>setV({...v,amount:Number(e.target.value)})}/><TextField type="date" label="Date" InputLabelProps={{shrink:true}} value={v.date} onChange={e=>setV({...v,date:e.target.value})}/><TextField multiline label="Description" value={v.description} onChange={e=>setV({...v,description:e.target.value})}/></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>Cancel</Button><Button variant="contained" onClick={save}>Save</Button></DialogActions></Dialog></Box>}
