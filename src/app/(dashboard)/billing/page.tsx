"use client";
import{useEffect,useState}from"react";import{Box,Card,CardContent,Typography,TextField,MenuItem,Table,TableBody,TableCell,TableHead,TableRow,Button}from"@mui/material";import{money}from"@/lib/utils";import{StatusChip}from"@/components/StatusChip";
export default function Billing(){const[students,setStudents]=useState<any[]>([]),[payments,setPayments]=useState<any[]>([]);const d=new Date();const[month,setMonth]=useState(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`);useEffect(()=>{fetch("/api/students").then(r=>r.json()).then(x=>setStudents(x.data||[]));fetch("/api/payments").then(r=>r.json()).then(x=>setPayments(x.data||[]))},[]);return <Box><Typography variant="h4" fontWeight={800}>Monthly Billing</Typography><Typography color="text.secondary" sx={{mb:3}}>Review one billing cycle and mark amounts collected through Payments.</Typography><Card variant="outlined"><CardContent><TextField type="month" label="Billing month" InputLabelProps={{shrink:true}} value={month} onChange={e=>setMonth(e.target.value)} sx={{mb:2}}/><Table><TableHead><TableRow><TableCell>Student</TableCell><TableCell>Room</TableCell><TableCell>Rent</TableCell><TableCell>Electricity</TableCell><TableCell>Total</TableCell><TableCell>Paid</TableCell><TableCell>Due</TableCell><TableCell>Status</TableCell></TableRow></TableHead><TableBody>{students.map(s=>{const p=payments.find(x=>x.studentId?._id===s._id&&x.billingMonth===month);const total=p?.totalAmount||s.monthlyRent;const paid=p?.amountPaid||0;return <TableRow key={s._id}><TableCell>{s.name}</TableCell><TableCell>{s.roomId?.roomNumber}</TableCell><TableCell>{money(p?.rentAmount||s.monthlyRent)}</TableCell><TableCell>{money(p?.electricityAmount||0)}</TableCell><TableCell>{money(total)}</TableCell><TableCell>{money(paid)}</TableCell><TableCell>{money(Math.max(total-paid,0))}</TableCell><TableCell><StatusChip status={p?.status||"pending"}/></TableCell></TableRow>})}</TableBody></Table></CardContent></Card></Box>}
