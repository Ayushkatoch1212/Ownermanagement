"use client";
import {useEffect,useState} from "react";import {Grid,Typography,Box,Card,CardContent,Table,TableBody,TableCell,TableHead,TableRow,LinearProgress,Chip} from "@mui/material";import ApartmentIcon from "@mui/icons-material/Apartment";import MeetingRoomIcon from "@mui/icons-material/MeetingRoom";import PeopleIcon from "@mui/icons-material/People";import PaymentsIcon from "@mui/icons-material/Payments";import {StatCard} from "@/components/StatCard";import {money} from "@/lib/utils";import {Animated} from "@/components/Animated";
export default function Dashboard(){const[d,setD]=useState<any>();useEffect(()=>{fetch("/api/dashboard").then(r=>r.json()).then(x=>setD(x.data))},[]);if(!d)return <Box><Typography variant="h4" fontWeight={800}>Dashboard</Typography><LinearProgress sx={{mt:2}}/></Box>;return <Animated><Box><Typography variant="h4" fontWeight={800}>Dashboard</Typography><Typography color="text.secondary" sx={{mb:3}}>Overview of your PG operations.</Typography><Grid container spacing={2}>{[[d.pgs,"Total PGs",<ApartmentIcon/>],[d.rooms,"Total Rooms",<MeetingRoomIcon/>],[d.students,"Active Students",<PeopleIcon/>],[d.occupancy+"%","Occupancy",<PeopleIcon/>],[money(d.collected),"Collected Revenue",<PaymentsIcon/>],[money(d.pending),"Pending Revenue",<PaymentsIcon/>],[money(d.expenses),"Expenses",<PaymentsIcon/>],[money(d.netIncome),"Net Income",<PaymentsIcon/>]].map((x:any,i)=><Grid key={i} size={{xs:12,sm:6,md:3}}><StatCard title={x[1]} value={x[0]} icon={x[2]}/></Grid>)}</Grid><Grid container spacing={2} sx={{mt:0}}><Grid size={{xs:12,md:8}}><Card variant="outlined"><CardContent><Typography fontWeight={700} sx={{mb:2}}>Revenue — last 6 months</Typography><Box sx={{display:"flex",alignItems:"end",gap:1,height:220}}>{d.revenue.map((r:any)=><Box key={r.month} sx={{flex:1,textAlign:"center"}}><Box sx={{height:170,display:"flex",alignItems:"end"}}><Box sx={{width:"100%",height:Math.max(8,Math.min(160,(r.revenue/(Math.max(...d.revenue.map((z:any)=>z.revenue),1)))*160)),bgcolor:"primary.main",borderRadius:1}}/></Box><Typography variant="caption">{r.month.slice(5)}</Typography></Box>)}</Box></CardContent></Card></Grid><Grid size={{xs:12,md:4}}><Card variant="outlined"><CardContent><Typography fontWeight={700}>Payment status</Typography>{Object.entries(d.status).map(([k,v]:any)=><Box key={k} sx={{display:"flex",justifyContent:"space-between",py:1.5}}><Chip label={k}/><Typography fontWeight={700}>{v}</Typography></Box>)}</CardContent></Card></Grid></Grid></Box></Animated>}
