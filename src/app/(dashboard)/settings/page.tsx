"use client";
import{useSession}from"next-auth/react";import{Box,Card,CardContent,Typography,TextField,Button}from"@mui/material";
export default function Settings(){const{data}=useSession();return <Box><Typography variant="h4" fontWeight={800}>Settings</Typography><Typography color="text.secondary" sx={{mb:3}}>Admin account settings.</Typography><Card variant="outlined" sx={{maxWidth:700}}><CardContent sx={{display:"grid",gap:2}}><TextField label="Name" value={data?.user?.name||""} InputProps={{readOnly:true}}/><TextField label="Email" value={data?.user?.email||""} InputProps={{readOnly:true}}/><Typography variant="body2" color="text.secondary">Password changes can be added with a dedicated secure endpoint. MongoDB credentials remain server-side.</Typography><Button variant="outlined">Security settings</Button></CardContent></Card></Box>}
