"use client";
import{useEffect,useState}from"react";import{Box,Grid,Card,CardContent,Typography}from"@mui/material";import{money}from"@/lib/utils";
export default function Reports(){const[d,setD]=useState<any>();useEffect(()=>{fetch("/api/dashboard").then(r=>r.json()).then(x=>setD(x.data))},[]);if(!d)return null;return <Box><Typography variant="h4" fontWeight={800}>Reports</Typography><Typography color="text.secondary" sx={{mb:3}}>Current portfolio summary.</Typography><Grid container spacing={2}>{[["Collected revenue",money(d.collected)],["Pending revenue",money(d.pending)],["Expenses",money(d.expenses)],["Net income",money(d.netIncome)],["Active students",d.students],["Rooms",d.rooms],["Capacity",d.capacity],["Occupancy",d.occupancy+"%"]].map(([a,b])=><Grid size={{xs:12,sm:6,md:3}} key={a}><Card variant="outlined"><CardContent><Typography color="text.secondary">{a}</Typography><Typography variant="h5" fontWeight={800} sx={{mt:1}}>{b}</Typography></CardContent></Card></Grid>)}</Grid></Box>}
