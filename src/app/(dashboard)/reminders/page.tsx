"use client";
import{useEffect,useState}from"react";import{Box,Button,Card,CardContent,Dialog,DialogTitle,DialogContent,DialogActions,TextField,Typography,Grid,Chip,MenuItem}from"@mui/material";importAddIcon from"@mui/icons-material/Add";
export default function Reminders(){const[data,setData]=useState<any[]>([]),[open,setOpen]=useState(false),[v,setV]=useState<any>({title:"",description:"",date:new Date().toISOString().slice(0,16),type:"custom"});async function load(){setData((await(await fetch("/api/reminders")).json()).data||[])}useEffect(()=>{load()},[]);async function save(){const r=await fetch("/api/reminders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});if(r.ok){setOpen(false);load()}}return <Box><Box sx={{display:"flex",justifyContent:"space-between",mb:3}}><Box><Typography variant="h4" fontWeight={800}>Reminders</Typography><Typography color="text.secondary">Rent, agreements and custom follow-ups.</Typography></Box><Button variant="contained" startIcon={<AddIcon/>} onClick={()=>setOpen(true)}>Add Reminder</Button></Box><Grid container spacing={2}>{data.map(x=><Grid size={{xs:12,md:6,lg:4}} key={x._id}><Card variant="outlined"><CardContent><Chip size="small" label={x.type}/><Typography variant="h6" fontWeight={700} sx={{mt:1}}>{x.title}</Typography><Typography color="text.secondary">{x.description}</Typography><Typography sx={{mt:2}}>{new Date(x.date).toLocaleString()}</Typography></CardContent></Card></Grid>)}</Grid><Dialog open={open} onClose={()=>setOpen(false)} fullWidth maxWidth="sm"><DialogTitle>Add Reminder</DialogTitle><DialogContent sx={{display:"grid",gap:2,pt:2}}><TextField label="Title" value={v.title} onChange={e=>setV({...v,title:e.target.value})}/><TextField multiline label="Description" value={v.description} onChange={e=>setV({...v,description:e.target.value})}/><TextField type="datetime-local" label="Date" InputLabelProps={{shrink:true}} value={v.date} onChange={e=>setV({...v,date:e.target.value})}/><TextField select label="Type" value={v.type} onChange={e=>setV({...v,type:e.target.value})}>{["rent","agreement","security","electricity","custom"].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>Cancel</Button><Button variant="contained" onClick={save}>Save</Button></DialogActions></Dialog></Box>}
