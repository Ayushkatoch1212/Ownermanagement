"use client";
import { useState } from "react";
import { Box,Paper,TextField,Button,Typography,Alert,Link as MuiLink } from "@mui/material";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { Animated } from "@/components/Animated";
export default function Signup(){const[v,setV]=useState({name:"",email:"",phone:"",password:"",confirmPassword:""});const[error,setError]=useState("");const[loading,setLoading]=useState(false);const router=useRouter();const change=(k:string)=>(e:any)=>setV({...v,[k]:e.target.value});async function submit(e:React.FormEvent){e.preventDefault();setError("");if(v.password!==v.confirmPassword)return setError("Passwords do not match");setLoading(true);const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});const d=await r.json();if(!r.ok)setError(d.message);else router.push("/login");setLoading(false)}return <Box sx={{minHeight:"100vh",display:"grid",placeItems:"center",p:2}}><Animated><Paper variant="outlined" sx={{p:{xs:3,sm:5},width:"100%",maxWidth:480}}><Typography variant="h4" fontWeight={800}>Create admin account</Typography><Typography color="text.secondary" sx={{mb:3}}>Set up your PG Manager account.</Typography>{error&&<Alert severity="error" sx={{mb:2}}>{error}</Alert>}<Box component="form" onSubmit={submit} sx={{display:"grid",gap:2}}>{[["name","Full name"],["email","Email"],["phone","Phone"]].map(([k,l])=><TextField key={k} label={l} value={(v as any)[k]} onChange={change(k)} required type={k==="email"?"email":"text"}/>)}<TextField label="Password" type="password" value={v.password} onChange={change("password")} required/><TextField label="Confirm password" type="password" value={v.confirmPassword} onChange={change("confirmPassword")} required/><Button variant="contained" size="large" type="submit" disabled={loading}>{loading?"Creating...":"Create account"}</Button></Box><Typography sx={{mt:3}} variant="body2">Already registered? <MuiLink component={Link} href="/login">Sign in</MuiLink></Typography></Paper></Animated></Box>}
